// Cookie consent UI removed per user request.
// Keep a no-op client component to avoid import errors where referenced.

'use client'

export default function CookieConsentBanner(): JSX.Element {
  return <></>
}
