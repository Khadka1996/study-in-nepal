'use client'

// WhatsApp button design removed and replaced with a no-op placeholder.
// This preserves the import used in layout without rendering UI.

export default function WhatsAppButton(): JSX.Element {
  return <></>
}
